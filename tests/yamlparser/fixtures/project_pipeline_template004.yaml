- job:
    name: test_job
    project-type: pipeline
    pipeline:
        script-path: flow.groovy
        scm:
            git:
                url: 'https://github.com/openstack-infra/jenkins-job-builder.git'
                credentials-id: 'some-github-credentials'
                ignore-notify: true
                shallow-clone: true
